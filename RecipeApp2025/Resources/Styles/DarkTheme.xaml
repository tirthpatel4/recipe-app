<?xml version="1.0" encoding="utf-8" ?>
<?xaml-comp compile="true" ?>

<ResourceDictionary xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="RecipeApp2025.Resources.Styles.DarkTheme">

    <Color x:Key="BackgroundColor">#0C1611</Color>
    <Color x:Key="PrimaryColor">#4E5E4E</Color>
    <!-- Warm brown-tan, subtle contrast -->
    <Color x:Key="AccentColor">#3BA79F</Color>
    <!-- Darkened teal for better contrast -->
    <Color x:Key="TextColor">#E3D9CF</Color>
    <!-- Soft beige, clear readability -->


    <Style TargetType="Button">
        <Setter Property="VisualStateManager.VisualStateGroups">
            <VisualStateGroupList>
                <VisualStateGroup x:Name="CommonStates">
                    <VisualState x:Name="Normal">
                        <VisualState.Setters>
                            <Setter Property="BackgroundColor" Value="{StaticResource AccentColor}" />
                            <Setter Property="TextColor" Value="{StaticResource TextColor}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="Disabled">
                        <VisualState.Setters>
                            <Setter Property="BackgroundColor" Value="#7F9E9B" />
                            <Setter Property="TextColor" Value="#F4F1EE" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="PointerOver" />
                </VisualStateGroup>
            </VisualStateGroupList>
        </Setter>
    </Style>

</ResourceDictionary>
    